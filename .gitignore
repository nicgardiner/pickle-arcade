# ── Dev quarantine: scratch/junk/dev-only files live here, never shipped ──
_dev/
_to_delete/

# ── Build / dependencies ──────────────────────────────
node_modules/
dist/
site/
.DS_Store
*.log

# ── Editor / OS cruft ─────────────────────────────────
*.bak
*.new
*.orig
*.tmp
*.recovered

# ── Dev-only / test artifacts (never ship to production) ──
test_*.txt
games.json.recovered

# ── Game-onboarder skill files (dev tooling, not part of the app) ──
*.skill
game-onboarder*
game-onboarder-workspace/
release-pusher/

# ── Dev notes (not part of the shipped app) ───────────
CLAUDE.md
PLAN-*.md
*-PLAN.md

# ── Personal data (must not be published) ─────────────
playerdata.json

# ── Stray scratch files ───────────────────────────────
zi6YlwEo
*-preview.html
terrain-explorer.html

# ── External (on-demand) games: hosted as GitHub release assets, never ──
# ── committed or bundled. Listed in games.json with "external": true. ──
black_knight_16.html
*.zip
